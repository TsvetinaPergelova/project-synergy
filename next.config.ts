import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  // i18n: {
  //   locales: ["default", "en", "bg", "fr"],
  //   defaultLocale: "bg",
  //   localeDetection: false,
  // },
  // trailingSlash: true,
  // Add other Next.js config options here if needed
};

export default nextConfig;
